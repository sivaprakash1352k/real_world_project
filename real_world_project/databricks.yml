# This is a Databricks asset bundle definition for real_world_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: real_world_project
  uuid: dfce9661-a2e3-40cd-88c3-c3b585db01b2

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: http://127.0.0.1:7073
    variables:
      catalog: dev_catalog
      schema: dev
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/sivaprakash.formal@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/sivaprakash.formal@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev_catalog
      schema: prod
    permissions:
      - user_name: sivaprakash.formal@gmail.com
        level: CAN_MANAGE
